{% comment %}
  Stellar Reverb – Signal Seeker CTA
  Call-to-action to join the Signal Seeker network.
{% endcomment %}
<section class="signal-seeker-cta container mx-auto text-center py-16 bg-black border-t border-purple-800">
  <h2 class="font-orbitron text-4xl mb-4 gradient-text">JOIN THE SIGNAL SEEKERS</h2>
  <p class="text-gray-300 mb-6">Gain early access to encrypted capsules, hidden relics, and classified lore fragments.</p>
  <div class="flex justify-center">
    {% form 'customer' %}
      <input type="hidden" name="contact[tags]" value="signal-seeker">
      <input type="email" name="contact[email]" placeholder="Enter transmission frequency (email)" class="px-4 py-2 rounded-l-md bg-gray-900 border border-purple-600 text-white focus:ring-2 focus:ring-pink-500">
      <button type="submit" class="px-6 py-2 bg-gradient-to-r from-pink-500 to-cyan-500 text-black font-orbitron rounded-r-md hover:from-cyan-400 hover:to-green-400 transition">Connect</button>
    {% endform %}
  </div>
</section>

{% schema %}
{
  "name": "Signal Seeker CTA",
  "settings": []
}
{% endschema %}