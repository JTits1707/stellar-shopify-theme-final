{% comment %}
  Stellar Reverb — Cosmic Network (Placeholder)
{% endcomment %}

<section class="cosmic-network" style="background:#0D0D0D; padding:30px; text-align:center; border:2px dashed #28A8D6; color:#00F0AC;">
  <h2>{{ section.settings.heading | default: "COSMIC NETWORK PLACEHOLDER" }}</h2>
  <p>{{ section.settings.subtext | default: "This will display networked lore, signals, or community features in the future." }}</p>
</section>

{% schema %}
{
  "name": "Cosmic Network",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "COSMIC NETWORK PLACEHOLDER"
    },
    {
      "type": "textarea",
      "id": "subtext",
      "label": "Subtext",
      "default": "This will display networked lore, signals, or community features in the future."
    }
  ],
  "presets": [
    {
      "name": "Cosmic Network"
    }
  ]
}
{% endschema %}