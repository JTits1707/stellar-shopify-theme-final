{% comment %}
  Placeholder for Cosmic Network
{% endcomment %}

<section class="cosmic-network" style="background:#0D0D0D; color:#00F0AC; padding:30px; text-align:center; border:2px dashed #28A8D6;">
  <h2>{{ section.settings.heading | default: "COSMIC NETWORK PLACEHOLDER" }}</h2>
  <p>{{ section.settings.subtext | default: "This section will eventually display networked lore, signals, or community features." }}</p>
</section>

{% schema %}
{
  "name": "Cosmic Network",
  "settings": [
    {
      "type": "text",
      "id": "heading",
      "label": "Heading",
      "default": "COSMIC NETWORK PLACEHOLDER"
    },
    {
      "type": "textarea",
      "id": "subtext",
      "label": "Subtext",
      "default": "This section will eventually display networked lore, signals, or community features."
    }
  ],
  "presets": [
    {
      "name": "Cosmic Network"
    }
  ]
}
{% endschema %}